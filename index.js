const Grammar = require('./components/Grammar');
const TextExpression = require('./components/TextExpression');
const TextSymbol = require('./components/TextSymbol');

module.exports = {
  Grammar,
  TextExpression,
  TextSymbol,
  default: Grammar,
};
